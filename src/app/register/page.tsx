'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Register(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState('');const r=useRouter();async function go(e:any){e.preventDefault();const x=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});if(x.ok)r.push('/');else setErr((await x.json()).error||'Error');}return <main className="wrap"><h1>Crear cuenta</h1><form className="stack" onSubmit={go}><input className="input" type="email" required placeholder="email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" type="password" minLength={6} required placeholder="contraseña (mín. 6)" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn">Crear cuenta</button>{err&&<div className="error">{err}</div>}</form></main>}
