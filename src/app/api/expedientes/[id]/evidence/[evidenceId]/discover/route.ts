import {NextResponse} from 'next/server';
import {prisma} from '@/src/lib/prisma';
import {currentUser} from '@/src/lib/auth';
export async function POST(_:Request,{params}:{params:Promise<{id:string;evidenceId:string}>}){const u=await currentUser();if(!u)return NextResponse.json({error:'No autenticado'},{status:401});const {id,evidenceId}=await params;const e=await prisma.evidence.findFirst({where:{id:evidenceId,expedienteId:id,expediente:{status:'PUBLISHED'}}});if(!e)return NextResponse.json({error:'Evidencia no disponible'},{status:404});const i=await prisma.investigation.findUnique({where:{userId_expedienteId:{userId:u.id,expedienteId:id}}});if(!i)return NextResponse.json({error:'Inicia la investigación primero'},{status:400});const count=await prisma.discovery.count({where:{userId:u.id,evidence:{expedienteId:id}}});if(e.unlockAfter>count)return NextResponse.json({error:'Evidencia bloqueada'},{status:403});const existed=await prisma.discovery.findUnique({where:{userId_evidenceId:{userId:u.id,evidenceId:e.id}}}); const d=existed ?? await prisma.discovery.create({data:{userId:u.id,evidenceId:e.id}});const total=await prisma.evidence.count({where:{expedienteId:id}});const found=await prisma.discovery.count({where:{userId:u.id,evidence:{expedienteId:id}}});const progress=total?Math.round(found/total*100):0;const status=progress>=100?'COMPLETED':'IN_PROGRESS';await prisma.investigation.update({where:{id:i.id},data:{progress,status}});const discoveries=await prisma.discovery.findMany({where:{userId:u.id,evidence:{expedienteId:id}},select:{evidenceId:true}});return NextResponse.json({ok:true,newlyDiscovered:!existed,progress,status,discoveredIds:discoveries.map(x=>x.evidenceId)});}
